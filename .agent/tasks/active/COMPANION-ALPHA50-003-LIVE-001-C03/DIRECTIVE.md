# CODEX DIRECTIVE — COMPANION-ALPHA50-003-LIVE-001-C03

## Objective

Deliver the first independently reconstructable **persistent-individual Alpha software boundary** on one green implementation tree.

Required production chain:

`authorized sensor-gateway evidence -> companion-core V2 memory/state -> body-neutral intent -> the same production Godot director/resolver/avatar/controller path used by normal embodiment -> observed Godot result -> V2 learning -> durable persistence -> restart -> remembered evidence changes later production behavior`

C03 is a continuation/correction. Do not return merely because a required component is missing or an ordinary test fails. Repair and continue unless an explicit Architect stop condition occurs.

## Authoritative basis

- Architecture v1.0 remains adopted.
- Phase 01 remains accepted.
- R06-C06 remains accepted only at its bounded frozen-art/controller/render/timing boundary.
- Architect Review 28: C02 PARTIAL ACCEPTED.
- Reviewed C02 task head: `9c83199a9beaf2fbb9ace037875035975fc8b12c`.
- C02 Alpha PR run `35055274200` failed the retained restart campaign with `0/500`, `identity=null`, and zero snapshots; this negative evidence must be preserved.
- Failed-run artifact: `10430534095`, ZIP SHA-256 `0d650aa6d47c4b984d2f11ab6308a9e69b3ed599032ba607fb5f8091fa307ed4`.
- Frozen R06 pack SHA-256: `1596bc28f2aac81344c4ba814a47deaa3f746e88e53278a81985977d41c8af40`.
- Review 28: `.agent/tasks/active/COMPANION-ALPHA50-003-LIVE-001-C03/ARCHITECT_REVIEW_28.md`.

Fetch and normally merge current `origin/main` before implementation.

## Retain

Retain without re-proving from scratch:

- V2-only acceptance direction and V1 guard;
- fixed-point V2 numerical state;
- sensor-gateway PID/UID/generation + HMAC ordinary authority;
- durable ordinary message reservation before V2 mutation;
- signed-64 sequence exhaustion refusal;
- real Godot 4.7.2 R06 render participation;
- shared transition-authority data + `MonTransitionResolver` scaffolding;
- frozen-pack metadata track lookup direction;
- version-neutral canonical contract aliases;
- accepted Phase 01 and bounded C06 regressions.

Do not treat the C02 resolver-only 10,000-case campaign as production transition acceptance.

## Workstream A — Repair the exact-head restart regression first

Before adding new resilience claims, reproduce the C02 failure from `35055274200` and determine exactly why the standalone harness obtains zero snapshots.

Repair the test/production precondition by driving the companion through a supported state-producing production path before restart. Do not restore a hidden background snapshot side effect solely to make the old harness green.

Required proof:

- preserve the C02 failing artifact/result as historical negative evidence;
- produce at least one durable V2 snapshot through supported production execution;
- stop/restart companion;
- restore the same identity/epoch/state;
- prove the repaired retained restart check passes;
- then retire the old restart-dominated 500-case script as the principal resilience claim and proceed to Workstream H.

The final Alpha workflow must be green. A final handoff with the Alpha workflow still failing is noncompliant unless an explicit Architect stop condition is reached.

## Workstream B — Make legacy ordinary file access compatibility-only

In all normal/default/Alpha resident modes, `ordinary-observation.json` must be ignored.

Only an explicit `COMPANION_PHASE01_COMPAT=1` compatibility mode may read it.

Required tests:

- default resident mode ignores a valid legacy file;
- Alpha mode ignores it;
- explicit Phase01 compatibility mode consumes it only where inherited tests require;
- authenticated sensor-gateway UDS continues to work after each negative;
- no V2 memory/state mutation occurs from the ignored file.

Do not use absence/presence of `COMPANION_ALPHA_LIFE` as the compatibility authority.

## Workstream C — Ground one transition authority in frozen pack semantics

The legal graph must not be free-standing data that can drift from the accepted pack.

Investigate the frozen pack/sidecar metadata and existing R06 authority for track roles, selection facing, entry/exit facing/posture, start/loop/stop roles, interruption/cancel semantics, and controller constraints.

Then use one of these bounded approaches:

1. derive/generated-compile `transition_authority.json` deterministically from frozen track metadata; or
2. if some relationship metadata is not present in the frozen pack, retain a separate immutable project transition-metadata file that references exact frozen track IDs/roles and is independently cross-validated against the pack.

In either case:

- no duplicated legality switch/table in runtime or verifier;
- authority compilation/validation fails on unknown track, facing/posture conflict, role conflict, impossible entry/exit, pack hash mismatch, or illegal interruption declaration;
- frozen image bytes remain unchanged.

## Workstream D — One production Godot execution path

Live body intents, transition qualification, and result learning must traverse the same production path.

Use the existing Godot presentation stack rather than a qualification-only direct resolver call or a separate single-track black-pack runner as the acceptance path.

Required path:

1. companion emits a typed intent;
2. bridge transports it without choosing the legal route;
3. real Godot 4.7.2 receives it through the production entry point;
4. `MonAnimationDirector`/shared resolver determines legality;
5. avatar/controller executes the concrete frozen route/tracks with the accepted single presentation clock;
6. representative rendering is observed at `RenderingServer.frame_post_draw`;
7. Godot emits a typed correlated result containing actual route/track sequence and terminal state;
8. bridge forwards the result unchanged except transport framing;
9. companion validates IDs/sequence/pack identity and applies outcome learning.

The Rust bridge may parse/validate contracts but may not select an action family/track/route as presentation authority.

### 10,000-case campaign

Run at least 10,000 deterministic requests through that **same full production Godot path**, accelerated/headless where appropriate.

Retain raw evidence for:

- start state;
- request/intent IDs;
- accepted route or fail-closed rejection;
- actual concrete tracks executed;
- entry/exit state;
- interruption/cancellation disposition;
- terminal state;
- pack identity;
- correlated result.

Required mutations/negatives:

- edge removed;
- illegal edge inserted;
- facing/posture conflict;
- interruption-range conflict;
- wrong pack/source hash;
- wrong/stale intent ID or sequence;
- runtime result says success but observed route disagrees;
- rejection followed by valid recovery.

Independent verification must reconstruct expectations from the frozen pack plus the authoritative compiled transition metadata, not from a copied runtime switch statement.

## Workstream E — Resident paired causal learning

Through real production processes only:

`sensor-gateway -> authenticated ordinary channel -> companion-core -> production Godot path -> observed result`

Create paired controls with identical non-target state and prove later production behavior differs for the intended causal reason in all four cases:

1. remembered preference;
2. corrected preference/belief while retaining superseded history;
3. pending unfinished commitment;
4. learned outcome/skill originating from a real Godot result.

For each pair publish:

- pre-state digest/fields;
- target evidence difference;
- arbitration scores/reason;
- chosen intent/action parameters;
- Godot result;
- post-state diff.

Direct calls to organism helpers alone do not qualify.

## Workstream F — Post-restart remembered behavior and body loss

### Remembered restart

After a qualifying interaction:

- terminate companion and Godot;
- restart both;
- restore same identity/epoch;
- supply later authenticated context;
- prove remembered/learned state changes the later action versus an otherwise-identical control;
- execute the changed action through real Godot.

### Godot loss

Kill Godot while companion remains alive and prove:

- canonical organism identity/state/memory/goals/commitments remain companion-owned;
- body availability becomes explicitly degraded;
- no fabricated successful result is recorded;
- Godot can restart/reconnect to the same individual.

## Workstream G — Bounded organism, memory, development and consolidation

Implement/qualify the minimum persistent-organism behavior required by R01/R02:

- drive-derived typed goal proposal;
- goal arbitration, continuation, completion, pause and resumption;
- commitments pending/completed/expired/revoked;
- action consequences including rest/satiation/recovery;
- retrieval respecting temporal validity, supersession, scope/privacy and confidence;
- unresolved contradiction -> abstention;
- evidence-gated development promotion;
- time-only development promotion rejection;
- consolidation over real typed V2 memories;
- episode closure;
- contradiction/generalization proposals;
- skill/preference/routine proposals when supported;
- unfinished-goal carry-forward;
- curiosity/question proposals;
- synthetic dream/rehearsal may never become external-event evidence.

Add an explicit dream-to-fact promotion negative.

### Rich isolated 30-day run

Run 30 simulated V2 days in an isolated store using the same production organism logic.

It must be non-degenerate: multiple action classes, changing drive regimes and real lifecycle transitions.

Include alternating user availability, rest cycles, commitment lifecycle transitions, at least two corrections, outcome feedback, skill progression, development positive/negative, consolidation cycles, contradiction/abstention, unfinished-goal resumption and model-worker absence.

Publish action/drive/goal/commitment/memory/skill/development/consolidation distributions and retained seed/traces. Simulation remains qualification evidence, not lived history.

## Workstream H — Genuine 500+ multi-class failure campaign

After Workstream A restores the basic restart invariant, execute at least 500 deterministic cases distributed across materially different failure classes.

Include as applicable:

- before event transaction;
- after event commit before snapshot;
- after snapshot before result acknowledgement;
- ordinary evidence processing;
- intent processing;
- result processing;
- companion SIGKILL;
- real Godot loss;
- care-core loss;
- supervisor restart;
- store unavailable;
- injected store corruption;
- interrupted backup/restore;
- model-worker loss/absence.

Use efficient scenario matrices/repetitions; do not spend the campaign on 500 copies of one graceful restart.

Verify no identity reseed, duplicate canonical event, duplicate outcome learning, silent commitment loss, provenance corruption, cross-authority write, or synthetic false evidence. Unrecoverable corruption must fail closed/degrade/freeze explicitly.

## Workstream I — Care process-loss independence

Using the real direct-care path:

1. process a synthetic safety candidate while companion runs;
2. kill companion;
3. send a new candidate and prove care processes it independently;
4. restore companion;
5. kill care-core;
6. prove ordinary companion life continues;
7. mark care coverage explicitly degraded;
8. make no assistance/notification-success claim;
9. restart care;
10. prove coverage recovery does not modify companion memory/goals.

No external notification delivery.

## Workstream J — Resident clean-root restore

After nontrivial V2 history exists:

- use SQLite Backup API;
- restore to a clean isolated XDG root;
- boot actual resident companion-core from that root;
- boot/reconnect real Godot;
- verify identity, epoch, internal variables, goals, commitments, memories/supersession, learned outcomes, skills, development and next legal intent sequence;
- execute a post-restore action through the production Godot path.

A JSON comparison alone is insufficient.

## Workstream K — Model-worker independence

If a real model-worker process exists, kill/withhold it during qualification. If none exists, prove from process topology and dependencies that identity, organism stepping, memory, arbitration, persistence and body-intent production do not require one. Do not create a fake worker solely for the test.

## Workstream L — Governed target-host sensors

Publish sanitized target-host evidence for the available webcam and microphone through `sensor-gateway` where permissions permit.

Webcam evidence: device identifier, selected format/resolution, bounded frame acquisition, timing/freshness, frame health/change/brightness metrics.

Microphone evidence: device identifier, format/sample rate, bounded sample acquisition, timing/freshness, level/activity metrics.

Raw media remains ephemeral. No semantic vision/person recognition/STT/TTS claim.

If access fails, preserve the exact host blocker and continue.

## Openbox

Do not reconfigure GNOME/mutter. The dedicated 1366x768 Openbox endurance remains BLOCKED unless already available without host reconfiguration. This does not stop the software work above.

## Evidence integrity and CI

Final evidence must include raw/reconstructable traces for every central workstream.

On one final implementation tree:

- Alpha/C03 workflow must be green;
- Phase 01 must be green;
- focused R06 must be green;
- inherited Phase 02 must be green;
- frozen source hash must remain exact;
- artifact hash manifest must be internally correct;
- failed C02 run/artifact remains preserved as negative history.

For PR-triggered checks, distinguish the synthetic PR merge commit from the task branch head. Either run an exact branch-head push workflow too, or prove tree equality explicitly; do not label a PR merge SHA as the exact task SHA.

## Do not change

No new/edited character art, frozen source pixels, unapproved runtime dependency, cloud authority, biometric identity, live external notification, model-owned canonical state/safety, safety routing through companion, multiple canonical writers, PR #9 merge, Issue #8 closure, or roadmap/completion self-acceptance.

## Stop and return to Architect only if

- potentially lived data requires destructive migration;
- Architecture v1.0 authority boundaries must change;
- an unavoidable central new runtime dependency is required;
- accepted art must change;
- required proof requires prohibited host reconfiguration;
- a second canonical writer is unavoidable;
- safety would need to route through companion.

A missing implementation, failed test or ordinary negative result is not itself a stop condition. Repair and continue.

## Acceptance criteria

C03 passes only when one final implementation tree establishes:

1. the C02 restart regression is repaired without hidden side effects;
2. legacy file ingress is explicit compatibility-only;
3. transition legality is cross-validated against frozen pack semantics;
4. live body intents and the 10k campaign traverse the same full production Godot director/avatar/controller/result path;
5. all four resident paired causal comparisons pass;
6. remembered evidence changes later behavior across full restart;
7. Godot loss/reconnect preserves organism truth;
8. goals/retrieval/development/consolidation are causally operative;
9. 30-day V2 behavior is non-degenerate;
10. 500+ genuine multi-class failure cases preserve required invariants;
11. care works without companion and companion degrades explicitly without care;
12. resident clean-root restore succeeds and executes through Godot;
13. model-worker independence is topology-qualified;
14. governed webcam/mic evidence is published or exact host blockers are preserved;
15. Alpha, Phase01, R06 and inherited Phase02 are green on a truthfully identified final tree;
16. raw artifacts permit independent reconstruction.

A full pass is expected to support an Architect completion assessment around **52–55%**, assigned only after independent review.

## Required project updates

Append-only update `.agent/CURRENT.md`, `.agent/INDEX.md`, C03 implementation/results, capability matrix, Notion coder report, PR #9 and Issue #8. Preserve Review 28 and the failed C02 Alpha evidence.

## Required handoff

Return exactly:

# CODEX RESULT — COMPANION-ALPHA50-003-LIVE-001-C03

with sections:

## Verdict
## Authority sync
## Protected work
## Baseline / merge / branch
## Retained C02 evidence and C02 failure reconciliation
## Restart regression repair
## Legacy live-ingress isolation
## Frozen-pack-grounded transition authority
## Unified production Godot execution path
## 10,000-case production transition campaign
## Resident paired causal comparisons
## Post-restart remembered-action proof
## Godot-loss continuity
## Goals / development / retrieval / consolidation
## Rich V2 30-day campaign
## Seeded multi-class failure campaign
## Care process-loss independence
## Resident clean-root restore
## Model-worker independence
## Governed webcam / microphone evidence or blockers
## Openbox blocker / endurance if available without reconfiguration
## Exact final-tree CI
## Raw artifacts / independent verifiers / hashes
## Corrected capability matrix
## Files changed
## Failures / blockers
## Notion / GitHub publication
## Commits / remote equality
## Recommendation

Then stop for Architect review.