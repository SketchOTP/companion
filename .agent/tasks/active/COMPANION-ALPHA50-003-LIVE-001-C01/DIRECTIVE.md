# CODEX DIRECTIVE — COMPANION-ALPHA50-003-LIVE-001-C01

## Objective

Close the remaining **software-only Alpha50 central gate** on one executable SHA.

This is a continuation/correction of `COMPANION-ALPHA50-003-LIVE-001`, not a new milestone. The prior handoff returned before the independently executable software criteria were completed. Do not return merely to report that a listed workstream was not implemented. Fix ordinary implementation failures, preserve negative evidence, and continue until the central acceptance boundary below is either proven or a listed Architect-stop condition occurs.

Required product-defining chain:

`authorized sensor-gateway ordinary evidence → companion-core V2 memory/state → body-neutral intent → real Godot 4.7.2 frozen-pack execution → typed observed result → V2 outcome learning → durable state → restart → remembered evidence changes later resident behavior`

## Why this is next

Review 26 accepts meaningful V2 and resident-process progress but found that ordinary evidence is not producer-authenticated, the observed body result is a Rust bridge echo rather than Godot execution, transition legality remains Python-owned, and failure/care-independence gates remain unexecuted.

No additional architecture layer is needed. The next decisive action is to wire and qualify the existing accepted boundaries correctly.

## Authoritative basis

- Architecture v1.0 remains adopted.
- Roadmap Phase 01 remains accepted.
- R06-C06 remains accepted at its bounded frozen-art/controller/render/timing boundary.
- Architect Review 26: `CONTINUE — COMPANION-ALPHA50-003-LIVE-001 PARTIAL ACCEPTED`.
- Reviewed implementation: `d484c0370512ab3bb9fbcebaf99b9e44a718cdba`.
- Reviewed publication: `299886beb2dac046e9ba5a25055f5f51c34eb9df`.
- Frozen R06 pack SHA-256: `1596bc28f2aac81344c4ba814a47deaa3f746e88e53278a81985977d41c8af40`.
- Review 26 Notion authority: `https://app.notion.com/p/3dc833cb27ff811abaa5c0b57c6530fe`.

Merge current `origin/main` normally before implementation.

## Known evidence to retain

Retain:

- no potentially lived V1 organism snapshots found in the read-only inspected XDG companion store;
- V2-only Alpha qualification and V1 acceptance guard;
- fixed-point V2 numerical organism state;
- preference/correction/commitment/outcome V2 methods;
- signed-64 sequence contract direction;
- separate ordinary-evidence UDS prototype;
- resident companion V2 event/intent/result-learning/snapshot plumbing;
- exact-head Alpha/Phase01/R06/inherited-Phase02 regressions;
- frozen accepted R06 source bytes.

Do not treat as accepted:

- current ordinary producer authentication;
- current Rust bridge echo as Godot execution;
- Python transition graph as production legality;
- repeated graceful restart as multi-class failure qualification;
- inherited care smoke as process-loss independence;
- current 30-day stepping as rich autonomy/development evidence.

## Scope

### A — bind ordinary evidence to the real authorized producer

Replace the same-UID + public digest acceptance rule with a separately provisioned ordinary-evidence authorization boundary.

Requirements:

1. The positive acceptance path must originate from the actual checked-in `sensor-gateway` process, not Python pretending to be it.
2. Reuse Phase-01-style supervisor provisioning and kernel/process identity where possible.
3. Use a dedicated ordinary-evidence capability/domain, separate from direct-care credentials. A suitable domain is `companion.ordinary-evidence.v1\0`.
4. Bind acceptance to the expected producer process/generation and effective UID; use PID credentials/pidfd and capability/HMAC semantics as appropriate to the existing architecture.
5. Canonicalize signed bytes deterministically before MAC verification.
6. `source` must be a typed/allowed producer identity, not any arbitrary nonempty string.
7. Enforce bounded freshness rather than `monotonic_ns > 0` only.
8. Persist replay/idempotency authority before organism mutation so duplicate message IDs remain rejected across companion restart.
9. A message accepted once must not mutate V2 state a second time after restart.
10. Required negatives:
   - same-UID unauthorized process;
   - spoofed `sensor-gateway` source;
   - stale/wrong producer generation;
   - bad capability/MAC;
   - replay in same process;
   - replay after companion restart;
   - malformed/unknown field;
   - stale/future freshness value.
11. Isolate `ordinary-observation.json` behind an explicit inherited compatibility/test mode that cannot be active in Alpha live operation. Alpha acceptance must fail if live mode can consume that file.
12. Preserve the independent direct-care safety route.

### B — fail closed on intent sequence exhaustion

The common wire range remains `1..=i64::MAX`.

At exhaustion:

- mark the organism/body-output capability degraded;
- emit no new canonical intent;
- return a typed refusal/error;
- never re-emit sequence `i64::MAX` under a new action;
- never wrap or reset the sequence.

Test max accepted and next rejected across schema/Rust/bridge/Godot.

### C — replace bridge echo with actual Godot execution

Use the already-qualified exact Godot 4.7.2 acquisition/runtime path from R06.

The Alpha acceptance path must start a real Godot process using the frozen accepted pack.

The Rust `godot-bridge` may remain a transport adapter, but it may not manufacture a completed result.

Required chain:

1. companion emits typed intent;
2. bridge transports it;
3. Godot production resolver receives it;
4. Godot selects an authorized frozen-pack route;
5. Godot executes the concrete track/frame path;
6. representative rendered evidence is observed after `RenderingServer.frame_post_draw`;
7. Godot emits the typed result;
8. bridge forwards it without redefining semantic truth;
9. companion validates correlation/sequence/source identity;
10. companion applies V2 outcome learning and persists evidence.

Result must include at minimum:

- intent ID/sequence;
- accepted/rejected;
- concrete track/path sequence;
- terminal presentation state;
- completion/cancellation reason;
- pack/source identity;
- correlation/causation IDs.

Add tamper negatives where a forged bridge result or mismatched intent/result is rejected.

### D — production Godot legal graph and 10,000-case campaign

Move legal transition authority out of Python.

1. Inventory accepted frozen track roles and transition semantics.
2. Implement an explicit fail-closed legal presentation graph in the production Godot presentation path or an already-accepted typed project resolver that Godot actually executes.
3. Preserve controller-owned locomotion and the single accepted presentation clock.
4. Enforce entry/exit facing/posture and interruption ranges.
5. Unsupported routes reject; no nearest-state substitution, fabricated connector, all-pairs shortcut, or teleport bypass.
6. If `AnimationTree.travel()` is used, project legality must be pre-resolved before the call.
7. Run at least **10,000 deterministic cases through a real Godot process**.
8. Retain raw per-case traces, not only summaries.
9. Render-observe representative connector, edge, interruption boundary, no-path, recovery, and cancellation cases.
10. Independent verifier derives legality from frozen pack + graph authority + raw trace.
11. Required mutations:
   - remove required edge;
   - inject all-pairs shortcut;
   - connector facing/posture mismatch;
   - interruption-range mutation;
   - pack/source mutation;
   - runtime claims success but observed path violates graph;
   - rejection followed by legal recovery.

Python may induce cases and verify output; it may not own the legal decision.

### E — resident paired causal comparisons through real producer and Godot

For each case, run paired controls through actual resident services with all unrelated canonical state held equal:

1. preference vs no preference;
2. correction vs stale prior preference;
3. pending commitment vs no commitment;
4. successful/failed observed Godot outcome vs control.

Each must produce a later action or action-parameter difference for the intended causal reason.

The correction must enter through typed ordinary evidence semantics, not a direct helper call only.

### F — post-restart remembered behavior proof

Run the full authenticated producer→companion→Godot→result path, persist it, terminate companion and Godot, restart both, and then supply later ordinary context.

Prove:

- same identity and organism epoch;
- relevant memory/learning survives;
- paired control without the remembered evidence chooses differently;
- real Godot executes the post-restart selected action.

### G — Godot-loss continuity

Kill the actual Godot process while companion remains alive.

Verify:

- canonical identity/state/memory/goals/commitments do not reset;
- body availability becomes explicitly degraded;
- no fabricated success result is generated;
- restart/reconnect Godot to the same individual;
- normal execution resumes.

### H — operative goals, development, retrieval, consolidation

Implement the minimum real R01/R02 behavior needed for the Alpha boundary:

- drive-derived typed goal proposal;
- goal arbitration/continuation/completion;
- unfinished-goal resumption;
- explicit commitment completion/expiration/revocation;
- retrieval respecting temporal validity, supersession, scope/privacy and confidence;
- unresolved current contradiction → abstain;
- evidence-gated development promotion;
- time-only promotion negative;
- consolidation over real typed V2 memories producing auditable supported proposals for episode closure, repetition/generalization, preference/routine or skill update, unfinished-goal carry-forward, and curiosity/question generation where evidence exists;
- synthetic dream/rehearsal → external factual memory mutation must reject.

### I — rich isolated V2 30-day campaign

Use production V2 logic against an isolated qualification root.

Include:

- present/absent user periods;
- competing drives;
- satiation/rest consequences;
- varied action distribution;
- commitments created/completed/expired/resumed;
- at least two corrections;
- outcome feedback;
- skill progression;
- valid development promotion and time-only rejection;
- consolidation cycles;
- contradiction/abstention;
- unfinished-goal carry/resume;
- model-worker-absent periods.

A campaign dominated by one permanent pending commitment or one repeated action does not pass.

### J — 500+ genuine multi-class failure cases

Run at least **500 deterministic cases distributed across materially different classes** using real production binaries and isolated stores.

Cover where supported:

- before event transaction;
- after event commit before snapshot;
- after snapshot before result acknowledgement;
- during ordinary-evidence processing;
- during intent/result processing;
- companion SIGKILL;
- real Godot loss;
- care-core loss;
- supervisor restart;
- authority store unavailable;
- injected store corruption;
- interrupted backup/restore;
- model-worker absence/loss if a worker process exists.

Retain class distribution and raw case records.

Verify no identity reseed, duplicate canonical application, duplicated learned outcome, silent commitment loss, invalid memory provenance, cross-authority write, or false recovery.

Graceful SIGTERM cases may remain as one class but cannot dominate the campaign.

### K — care process-loss independence

Using the real direct-care path:

1. send a synthetic safety candidate with companion running;
2. kill companion;
3. send a new candidate and prove care still receives/processes it;
4. restart companion;
5. kill care;
6. prove ordinary companion V2 life continues;
7. care coverage becomes explicitly degraded;
8. no assistance/notification success claim is emitted;
9. restart care;
10. prove care coverage recovers without changing companion memory/goals.

### L — resident clean-root restore

After nontrivial V2 history exists:

- use approved SQLite Backup API;
- restore to a clean isolated XDG root;
- start the actual resident companion service against the restored root;
- start/reconnect Godot;
- prove same identity/epoch and preservation of memories, supersession, commitments, learned outcomes, skills, development and next legal sequence;
- execute one post-restore action successfully.

JSON equality alone does not pass.

### M — model-worker independence

If a model-worker process already exists, kill/withhold it and prove core V2 life continues.

If no model-worker process exists yet, do not invent one solely for the test. Instead prove from actual supervisor/process topology that no model worker is required for identity, drives, memory, action selection, persistence or Godot intent production, and report the boundary truthfully.

### N — bounded real sensor acquisition on the target Linux host

The host currently exposes video and audio devices, so inventory-only evidence is no longer sufficient unless device access itself fails.

Without host reconfiguration:

- use `sensor-gateway` to open an available webcam and acquire bounded low-level frames;
- record device identity, format/resolution, timing/freshness and low-level frame health/change/brightness metadata;
- use `sensor-gateway` to acquire bounded microphone samples with format/rate/timing/level metadata;
- keep raw media ephemeral by default;
- no object/person recognition, STT or semantic vision claim.

If device permissions or runtime APIs block access, record the exact failure and continue.

Openbox remains a separate physical blocker; do not reconfigure GNOME/mutter to simulate the authorized target.

### O — artifact and CI integrity

On one executable SHA:

- Alpha live correction workflow passes;
- Phase 01 passes;
- focused R06 passes;
- inherited Phase 02 passes;
- frozen source hash remains exact.

Upload raw trace directories for:

- ordinary auth negatives/positives;
- resident paired causality;
- real Godot integration;
- 10k transitions;
- failure matrix;
- care process loss;
- rich 30-day campaign;
- restore;
- sensor acquisition/blockers.

Do not reduce these to summary JSON only.

Fix the evidence hash manifest so it does not hash its own just-truncated output as the empty-file SHA. Publish manifest digest separately or exclude the manifest itself.

## Do not change

Do not:

- generate/modify character art;
- alter frozen source pixels;
- adopt a new runtime dependency without Architect approval;
- make models authoritative;
- route safety through companion;
- add biometrics or live external notifications;
- reconfigure the desktop/Openbox target;
- merge PR #9 or close Issue #8;
- self-accept Alpha50/Phase02 or assign completion percentage.

## Stop and return to Architect if

Return early only if:

- potentially lived state requires destructive migration;
- Architecture v1.0 authority boundaries must change;
- a central unavoidable new runtime dependency is required;
- accepted source art must change;
- required qualification needs prohibited host reconfiguration;
- a second canonical writer would be required;
- safety would have to route through companion.

A failing test, missing implementation, bug, or negative result is not by itself a stop condition. Repair it and continue when within this directive.

## Acceptance criteria

Pass only if the final exact executable tree establishes the central software boundary:

1. ordinary evidence is bound to the authorized real producer and rejects unauthorized same-UID spoofing;
2. replay/idempotency remains correct across companion restart;
3. legacy file ingress is inaccessible in Alpha live mode;
4. intent sequence exhaustion refuses further emission without duplication;
5. real Godot executes companion intents and returns typed observed results;
6. V2 learning consumes those real results;
7. four resident paired causal comparisons pass;
8. post-restart remembered evidence changes later behavior through real Godot;
9. Godot loss/reconnect preserves organism truth;
10. production Godot owns/executes legal transitions and passes 10,000+ independently verified cases;
11. goals/development/retrieval/consolidation are causally operative at the bounded Alpha level;
12. rich V2 30-day campaign is non-degenerate;
13. 500+ failures cover genuine multi-class interruption/failure points;
14. care independently survives companion loss and companion explicitly degrades through care loss;
15. resident Backup API clean-root restore proves the same individual in running services;
16. core life is model-worker independent;
17. available target camera/mic are actually probed/acquired or return explicit access blockers;
18. exact-head foundation/R06/Phase02 regressions remain green;
19. raw artifacts are sufficient for independent reconstruction.

If all central criteria pass independent Architect review, the expected overall project assessment is approximately **50–53%**. Openbox may remain an explicit physical-host blocker if unavailable without reconfiguration.

## Required project updates

Append-only update:

- `.agent/CURRENT.md`;
- `.agent/INDEX.md`;
- active C01 implementation/results;
- capability matrix;
- Notion Alpha report if connector is available;
- PR #9 and Issue #8.

Preserve all earlier negative evidence and reviews.

## Required handoff

Return exactly:

# CODEX RESULT — COMPANION-ALPHA50-003-LIVE-001-C01

with sections:

## Verdict
## Authority sync
## Protected work
## Baseline / merge / branch
## Retained Alpha50-003 evidence
## Ordinary producer authentication and durable replay rejection
## Intent-sequence exhaustion behavior
## Actual Godot process integration
## Resident paired causal comparisons
## Post-restart remembered-action proof
## Godot-loss continuity
## Production Godot legal graph and 10,000-case campaign
## Goals / development / retrieval / consolidation
## Rich V2 30-day campaign
## Seeded multi-class failure campaign
## Care process-loss independence
## Resident clean-root restore
## Model-worker independence
## Target webcam / microphone acquisition or exact blockers
## Openbox blocker / endurance if target becomes available without reconfiguration
## Exact-head CI
## Raw artifacts / independent verifiers / hashes
## Corrected capability matrix
## Files changed
## Failures / blockers
## Notion / GitHub publication
## Commits / remote equality
## Recommendation

Then stop for Architect review.
