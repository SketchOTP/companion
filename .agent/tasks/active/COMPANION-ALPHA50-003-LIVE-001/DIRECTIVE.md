# CODEX DIRECTIVE — COMPANION-ALPHA50-003-LIVE-001

## Objective

Prove one **continuous V2 individual through the real production service graph**.

Replace the remaining Alpha50 V1/proxy/file-side-channel acceptance evidence with one integrated path:

`authorized ordinary evidence → companion-core V2 organism/memory → body-neutral intent → real Godot production routing/execution → typed observed result → V2 outcome learning → durable state → process restart → remembered evidence changes later production behavior`

The same final implementation must also move legal transition qualification into real Godot, execute real multi-class failure qualification, and prove care-core independence under process loss.

This is a software integration acceptance campaign, not another data-structure campaign. Do not return merely because fixed-point types, test-only helpers, or Python models pass.

Physical webcam/microphone/Openbox resources are secondary in this directive. Probe them without reconfiguration and preserve exact blockers, but their genuine absence does not authorize stopping independently executable software integration work.

## Why this is next

Alpha50-002 corrected the most serious canonical numeric defect and placed V2 state inside resident `companion-core`. However, independent Review 25 found that the Alpha artifact still obtains most organism/memory evidence from V1 and that the defining product loop never reaches Godot or feeds an observed result back into V2 learning.

The next decisive proof is not a larger unit-test matrix. It is causal continuity across actual process and authority boundaries.

## Authoritative basis

- Architecture v1.0 remains adopted.
- Roadmap Phase 01 remains accepted.
- R06-C06 remains accepted at its bounded frozen-art/controller/render/timing boundary.
- Architect Review 25: `CONTINUE — COMPANION-ALPHA50-002 PARTIAL ACCEPTED`.
- Review 25 Notion: https://app.notion.com/p/3dc833cb27ff8128a1f8cb920146a791
- Review 25 repository: `.agent/tasks/active/COMPANION-ALPHA50-003-LIVE-001/ARCHITECT_REVIEW_25.md`.
- Reviewed executable head: `70b2a5ca51a7c8adb688a97e42243f88c36cc5fc`.
- Reviewed publication head: `3d05123f9d1c04a9605f12fc6e42ed4c585ca88b`.
- Reviewed Alpha artifact `10387094480`, ZIP SHA-256 `d032bc5e436a470c40f43e337e6eaa49fc83e7e60d71c7babde18d6286f95395`.
- Frozen R06 pack SHA-256 `1596bc28f2aac81344c4ba814a47deaa3f746e88e53278a81985977d41c8af40`.
- R01 remains organism/autonomy/development authority.
- R02 remains memory/learning/consolidation authority.

Fetch and normally merge current `origin/main` before implementation.

## Known evidence

Retain:

- V2 bounded milli-integer physiology/drives/skills;
- deterministic V2 preference/outcome-aware scoring;
- V2 action-result confidence/stress/skill updates;
- resident V2 restore/step/snapshot integration;
- current local preference-memory plumbing as a useful intermediate implementation, but not accepted ingress evidence;
- graceful V2 restart continuity;
- fail-closed scanner self-test;
- exact executable-head Alpha/Phase01/R06/Phase02 green CI;
- all accepted C06 evidence and frozen source identity;
- direct-care authenticated transport foundation.

Do not use the following as acceptance substitutes:

- V1 organism/memory qualification;
- direct calls from qualification code into organism methods;
- `ordinary-observation.json` as the accepted ordinary producer boundary;
- Python-owned presentation legality;
- 500 copies of graceful SIGTERM restart as multi-class failure evidence;
- inherited direct-care smoke as process-loss independence;
- generic/underconstrained V2 nested schema fields.

## Scope

### 1 — Protect potentially lived state before any migration

Before writing or migrating canonical state:

1. resolve the **actual** configured/default XDG data/state/runtime roots used by the project Linux host;
2. inspect relevant companion authority database metadata read-only;
3. determine whether any potentially lived/non-test V1 organism snapshot/state exists outside the repository;
4. record paths in redacted/non-secret form and exact disposition.

If potentially lived V1 organism state exists, do not destructively migrate, overwrite, reseed, or silently ignore it. Stop only migration/activation work and return to Architect if preserving it requires a migration authority decision. Continue isolated qualification work where safe.

Repository search alone is insufficient.

### 2 — Finish the V2 canonical contract

V2 must be the only organism implementation used by new Alpha acceptance tests.

Create fully typed, fail-closed V2 representations/schema for at minimum:

- canonical identity/epoch/tick/lifecycle;
- fixed-point internal state;
- drives;
- goals;
- commitments;
- typed memories/evidence references;
- skills/development state;
- learned preferences/outcomes;
- degradation;
- last body-neutral intent.

`goals`, `commitments`, `memories`, and `last_intent` may not remain unconstrained generic arrays/objects in the acceptance schema.

Carry the accepted common Rust/Godot signed-64 sequence ceiling into organism-generated body intents. Add exact max accepted and max+1 rejected Rust/schema/Godot boundary evidence.

V1 may remain only as historical fixture/negative compatibility evidence. The Alpha50 acceptance workflow must fail if production qualification accidentally instantiates V1.

### 3 — Implement the authenticated ordinary evidence boundary

Do not route ordinary evidence over the direct-care safety socket.

Extend/reuse the accepted supervisor/IPC foundation to create a **separately addressed ordinary evidence channel** from an authorized producer/sensor-gateway boundary into `companion-core`.

Required envelope semantics include at minimum:

- schema version;
- immutable message/event ID;
- producer identity/generation;
- source type;
- monotonic/freshness evidence;
- confidence/quality where applicable;
- replay/idempotency protection;
- causation/correlation;
- typed ordinary payload.

Use kernel/process/capability authorization consistent with Architecture v1.0 / Phase 01 patterns. Do not trust message-declared identity alone.

Safety candidates must continue to reach care-core independently and must not transit companion-core.

Remove `ordinary-observation.json` from the acceptance path. It may remain only as an explicitly non-production diagnostic fixture if still useful.

### 4 — Make V2 memory and learning causally complete

Implement V2-native memory operations; do not call V1 methods for acceptance.

Required paired-control cases through resident services:

1. **Preference** — ordinary evidence stores a typed preference and changes a later body action/parameter versus an otherwise-identical control.
2. **Correction** — a second ordinary evidence event supersedes an earlier belief/preference, preserves history, and changes later production behavior versus the pre-correction control.
3. **Commitment** — a durable pending commitment changes later selection versus identical state without the commitment; complete/expire/revoke transitions are explicit.
4. **Observed outcome** — a real Godot result changes V2 learned outcome/skill state and then changes later action/action-parameter choice versus a control without that result.

For every comparison publish the full differing causal fields and prove no unrelated state drift explains the decision.

### 5 — Implement operative goals, development, and consolidation

At minimum for Alpha acceptance:

- drives can propose or influence typed goals;
- an active goal participates in selection rather than only appearing as metadata;
- goal completion/resumption is explicit;
- one development capability promotion requires measurable competence/stability evidence;
- a time-only promotion attempt fails;
- consolidation consumes real V2 typed memories and emits auditable proposal records;
- include episode closure, contradiction handling, one preference/routine or skill proposal, unfinished-goal carry-forward, and one curiosity/question proposal where supported;
- factual promotions retain supporting evidence;
- attempt to promote synthetic dream/rehearsal content as an external factual event must fail.

This need not be a complete lifelong development system. It must prove the architecture is operative rather than structural.

### 6 — Implement the typed companion ↔ Godot execution channel

Extend the accepted bridge instead of inventing a test-only bypass.

`companion-core` must send typed body-neutral intents to the real Godot process with:

- intent ID;
- signed-range intent sequence;
- causation/correlation IDs;
- action semantic;
- requested facing/context where applicable;
- canonical source state/event references needed for audit, without giving Godot organism authority.

Godot returns a typed observed result containing at least:

- intent ID/sequence;
- accepted/rejected execution state;
- concrete track/path sequence;
- terminal presentation state;
- completion/cancellation reason;
- observed source/pack identity;
- result correlation/causation.

`companion-core`, not Godot, interprets that result as outcome evidence and updates learning/state.

### 7 — Execute one complete remembered-action production trace

Using real resident services and real Godot:

1. send an authenticated synthetic ordinary observation over the ordinary producer boundary;
2. companion accepts canonical event evidence;
3. V2 memory/state changes;
4. V2 selects a body-neutral action;
5. intent crosses the real bridge;
6. Godot routes and executes the frozen approved presentation;
7. result returns;
8. companion writes result/outcome evidence and updates V2 skill/learned state;
9. persist/snapshot state;
10. kill/stop companion and Godot;
11. restart both;
12. same identity and epoch restore;
13. send later context;
14. paired control proves remembered evidence changes the later production action.

Retain raw cross-process trace IDs so the Architect can reconstruct the causal chain without trusting summary PASS fields.

### 8 — Put legal transition authority in production routing

Python may generate scenarios and independently verify traces. It may not define the legal graph used by production.

Derive/load production transition authority from the frozen accepted pack plus explicit project-owned role metadata. Do not infer all-pairs legality from matching names.

Required behavior:

- exact entry/exit facing/posture composition;
- start/loop/stop continuity;
- declared interruption constraints;
- no-path rejection;
- no silent substitution;
- no teleport shortcut;
- no second animation clock;
- controller remains movement authority.

Run at least **10,000 deterministic cases through the production resolver inside a real Godot process**. Headless/accelerated operation is permitted.

Raw per-case evidence must include start state, request, decision, concrete observed track sequence, terminal state, interruption disposition, intent/result IDs and pack identity.

Render-observe representative cases for every material connector/edge/interruption/no-path/recovery class.

Independent verifier must reconstruct legality from source authority and traces.

Required tamper negatives:

- remove required edge;
- inject all-pairs shortcut;
- facing/posture mismatch;
- interruption-range mutation;
- source/pack identity mutation;
- runtime-success vs observed-path contradiction;
- invalid rejection followed by valid recovery.

### 9 — Rich V2 30-day isolated qualification

Rewrite the Alpha long-horizon campaign so it uses only V2 resident/domain logic in an isolated qualification root.

Across 30 simulated organism days include:

- user-present and absent periods;
- multiple competing drive states;
- action consequences and satiation/recovery;
- created/completed/expired/resumed commitments;
- ordinary memories and at least two corrections;
- Godot-result/outcome feedback or production-equivalent result fixtures routed through the same companion result handler;
- skill progression;
- successful evidence-gated development;
- rejected time-only development;
- consolidation cycles;
- contradiction + abstention;
- unfinished goal carried forward then resumed;
- model-worker outage periods.

Report distributions and state transitions. Simulated evidence must remain isolated and never be imported into a lived/runtime store.

### 10 — Seeded multi-class failure campaign

Run at least **500 retained deterministic cases distributed across genuine failure classes**, including as supported by existing injection points:

- before ordinary event commit;
- after event commit before organism snapshot;
- after snapshot before intent publication/ack;
- during intent/result handling;
- companion SIGKILL;
- Godot loss/restart;
- care loss/restart;
- supervisor restart;
- store unavailable;
- store corruption;
- interrupted backup/restore;
- model-worker loss.

Do not count 500 orderly SIGTERM restarts as this campaign.

Verify exact recovery invariants: identity/epoch, event idempotency, no duplicated outcome application, commitment/memory provenance continuity, explicit frozen/degraded store behavior, no cross-authority writes.

### 11 — Prove care-core process independence

With the real direct-care transport:

1. safety candidate accepted while companion is running;
2. kill companion;
3. a new safety candidate is still processed and stored by care;
4. restart companion;
5. kill care;
6. ordinary companion life and V2 persistence continue;
7. care coverage becomes explicitly degraded and no assistance claim is emitted;
8. restart care;
9. coverage recovers without altering companion memory/goals.

No live notification/provider delivery.

### 12 — Godot/model/backup continuity

Separately prove:

- killing Godot cannot mutate/reset canonical organism/memory truth;
- Godot restart reconnects to the same individual;
- absence of model workers does not stop identity, physiology, drives, goals, basic memory, action selection, body intents or persistence;
- after nontrivial V2 memories/corrections/commitments/skills/development exist, SQLite Backup API export + clean-root restore starts the **actual resident companion-core** and restores the same V2 individual.

Do not validate restore only by JSON equality in a qualification binary.

### 13 — Physical target probes

On the actual project Linux PC, without reconfiguration, probe existing:

- webcam;
- microphone;
- logical X/Openbox target.

If devices are accessible, acquire bounded low-level samples through sensor-gateway. No semantic vision/STT/TTS claim.

If Openbox target is accessible, run the two-hour 1366×768 resident endurance.

If unavailable, record exact blocker and continue. Physical blocker remains BLOCKED, not PASS.

### 14 — Evidence integrity

The final acceptance artifact must contain raw machine-readable traces for:

- ordinary ingress;
- remembered-action vertical slice;
- paired causal comparisons;
- Godot transition campaign;
- failure matrix;
- care process-loss campaign;
- V2 long-horizon simulation;
- backup/restore continuity.

Summary JSON is secondary.

Alpha acceptance CI must fail if:

- V1 is instantiated in the acceptance qualification path;
- the secret scanner cannot execute;
- raw required trace groups are missing;
- transition legality is Python-owned;
- required process-loss/failure classes were not executed.

Correct the capability matrix: `embodiment_habitat` must be bounded C06 candidate/accepted-runtime evidence, not broad Phase-02/product acceptance.

## Do not change

Do not:

- generate or modify character art;
- change frozen source pixels or accepted pack identity;
- adopt a new runtime framework/dependency without Architect review;
- use cloud services as canonical authority;
- implement biometric identity;
- add live external notification;
- let models own state, memory, identity, consent, goals, tools, transition legality or safety decisions;
- perform uncontrolled online foundation-model weight updates;
- route safety through companion-core;
- create a shared writable companion/care database;
- merge PR #9;
- close Issue #8;
- self-accept a roadmap phase or completion percentage.

## Required investigation

Before implementation:

1. inspect actual XDG authority roots read-only for V1/lived state;
2. map V2 schema fields to concrete Rust types and signed wire bounds;
3. trace supervisor IPC provisioning and design the separate ordinary evidence channel without weakening direct-care independence;
4. trace current Godot bridge and production R06 director so the typed intent/result path extends existing authority rather than bypassing it;
5. inventory frozen track roles and interruption metadata;
6. inventory existing failure injection hooks and store fault behavior;
7. determine actual target-host hardware/display access separately from hosted CI.

## External discovery

No new dependency is expected or pre-authorized.

Use current official Godot/SQLite/Linux documentation only where implementation details require it. If `AnimationTree` is used, project legality must be pre-checked before `travel()`; engine no-path teleport behavior may not define project transition legality.

If a central unavoidable new dependency or architecture change is discovered, stop the dependent work and return to Architect. Continue independent safe work where possible.

## Authority boundaries

- `companion-core`: canonical identity/epoch, organism/world state, needs/drives/goals, ordinary accepted evidence, typed memories, preferences/relationships, development/skills, body-neutral intents, and interpretation of body results.
- `care-core`: authenticated safety receipts, policy/incident lifecycle and care degradation.
- `identity-consent-vault`: consent/contact/secret authority; unchanged.
- `sensor-gateway`: device lease, ephemeral raw buffers, quality/health, separately addressed ordinary observations and safety candidates.
- Godot: presentation routing/execution/session observations only.
- model workers: optional bounded inference only.
- supervisor: lifecycle/readiness/channel provisioning/backup coordination, not domain-event authority.
- Python qualification: scenario induction and independent verification only, never production decision authority.
- Codex: implementation/evidence authority only; no self-acceptance.

## Acceptance criteria

`COMPANION-ALPHA50-003-LIVE-001` passes only when one final implementation tree establishes all of the following central software criteria:

1. no potentially lived V1 state is destructively touched; actual XDG disposition is documented;
2. V2 canonical schema/types are fully bounded and typed for organism/memory/goal/commitment/intent state;
3. V1 is excluded from Alpha acceptance execution;
4. organism-generated intent sequence obeys common signed-64 Rust/Godot bounds;
5. authorized ordinary evidence enters over the real authenticated ordinary producer boundary;
6. safety path remains independently direct to care;
7. V2 preference causes changed later resident production behavior;
8. V2 correction causes changed later resident production behavior while preserving superseded history;
9. V2 commitment causes changed later behavior and has explicit lifecycle transitions;
10. a real Godot execution result changes V2 learning and later production choice;
11. operative goal and evidence-gated development behavior is demonstrated;
12. V2 consolidation is provenance-bound and false dream→external-fact promotion is rejected;
13. one complete resident ordinary→memory→intent→Godot→result→learning→snapshot trace executes;
14. after companion+Godot restart, same individual restores and remembered evidence changes later production action versus paired control;
15. Godot loss cannot reset canonical truth;
16. production Godot executes and traces 10,000+ transition cases with independent verification and required tamper negatives;
17. rich 30-day isolated V2 campaign exercises memory/learning/goals/development/consolidation rather than only elapsed steps;
18. 500+ retained deterministic cases span genuine multi-class failure injection;
19. care processes new safety evidence while companion is unavailable;
20. companion continues with explicit care degradation while care is unavailable;
21. resident V2 Backup API clean-root restore preserves the nontrivial individual;
22. resident basic life continues without model workers;
23. exact-head Phase 01/R06/retained Phase 02 regressions remain green;
24. raw evidence artifacts are independently reconstructable and hash-bound;
25. capability matrix uses truthful bounded labels.

If all 25 central criteria pass independent Architect review, the intended evidence-grounded overall project assessment is approximately **50–55%**, even if genuinely unavailable physical webcam/microphone/Openbox resources remain explicitly BLOCKED.

## Required validation

- full V2 schema + Rust round-trip and negative bounds;
- signed sequence max/max+1 Rust/schema/Godot tests;
- authenticated ordinary-channel authorization/replay/idempotency negatives;
- four resident paired causal comparisons;
- goal/development/consolidation positives and negatives;
- complete cross-process remembered-action trace;
- restart remembered-action trace;
- Godot-loss continuity;
- 10,000+ real Godot transition cases + independent verifier + tamper matrix;
- rich V2 30-day isolated campaign;
- 500+ multi-class failure traces;
- kill-companion/kill-care independence traces;
- model-worker outage;
- meaningful-state resident clean-root restore;
- exact-head CI and raw artifact hashes.

## Prohibited claims

Do not claim semantic vision, STT, TTS, biometric identity, live emergency efficacy, real external delivery, security certification, reliability/SLA, lifetime durability, pilot/release readiness, Phase 02 acceptance, Alpha acceptance, 50% completion, or full project completion before Architect review.

## Stop and return to Architect if

Return early only if:

- potentially lived V1 state requires a destructive/nontrivial migration decision;
- the separate ordinary channel or body-result path requires changing Architecture v1.0 authority boundaries;
- an unavoidable new runtime dependency is central;
- approved character pixels must change;
- required target-host evidence would require prohibited host reconfiguration;
- continuing would create multiple canonical writers or route safety through companion.

Ordinary implementation/test failures are not stop conditions. Preserve them and continue/repair within this directive.

## Required project updates

Update append-only:

- `.agent/CURRENT.md`;
- `.agent/INDEX.md`;
- Alpha task implementation/outcome records;
- corrected capability matrix;
- Notion Alpha report;
- PR #9;
- Issue #8.

Preserve Review 24, Review 25, Alpha50-001/002 evidence and all negative results.

## Required handoff

Return exactly:

# CODEX RESULT — COMPANION-ALPHA50-003-LIVE-001

with sections:

## Verdict
## Authority sync
## Protected work
## Baseline / merge / branch
## Actual XDG V1/lived-state disposition
## Fully typed canonical organism V2 and signed wire bounds
## Authenticated ordinary evidence boundary
## Preference / correction / commitment / observed-outcome paired production causality
## Goals / development / consolidation / dream-boundary evidence
## Complete ordinary → companion V2 → Godot → result → learning trace
## Restart remembered-action paired trace
## Godot-loss continuity
## Production Godot legal graph and 10,000-case campaign
## Rich V2 30-day isolated campaign
## Seeded multi-class failure campaign
## Care process-loss independence campaign
## Model-worker outage
## Resident Backup API clean-root restore
## Target webcam / microphone result or blocker
## Openbox endurance result or blocker
## Exact-head CI
## Raw artifacts / hashes / independent verifier
## Corrected capability matrix
## Files changed
## Failures / blockers
## Notion / GitHub publication
## Commits / remote equality
## Recommendation

Then stop for Architect review.
