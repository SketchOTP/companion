# CODEX DIRECTIVE — COMPANION-ALPHA50-003-LIVE-001-C02

## Objective

Close the remaining **central software Alpha50 gate** on one executable SHA.

Primary proof:

`authorized sensor-gateway evidence -> companion-core V2 memory/state -> production body intent -> same production Godot resolver/director used by normal live embodiment -> observed Godot result -> V2 learning -> durable persistence -> restart -> remembered evidence changes later production behavior`

This is a continuation/correction. Do not return because a required workstream is merely missing or a test initially fails. Repair and continue unless an explicit Architect stop condition occurs.

## Authoritative basis

- Architecture v1.0 adopted.
- Phase 01 accepted.
- R06-C06 accepted only at its bounded frozen-art/controller/render/timing boundary.
- Architect Review 27: C01 PARTIAL ACCEPTED.
- Reviewed executable: `794571f10e6bec1814af6b7d27da227a234f2566`.
- Reviewed publication: `113d747cdfba2d33c8673d32233cd537448aa4ab`.
- Alpha artifact: `10424275946`, independently verified ZIP SHA-256 `7e5bada75cfff0e87ab6693898604fc71b5374775e448460473f7c31fd143e43`.
- Frozen R06 pack: `1596bc28f2aac81344c4ba814a47deaa3f746e88e53278a81985977d41c8af40`.
- Review 27: `.agent/tasks/active/COMPANION-ALPHA50-003-LIVE-001-C02/ARCHITECT_REVIEW_27.md`.

Fetch and normally merge current `origin/main` before implementation.

## Retain

Retain C01's bounded advances:

- V2-only acceptance execution;
- fixed-point V2 numerical state;
- sensor-gateway PID/UID/generation + HMAC ordinary authority;
- durable ordinary message reservation before V2 mutation;
- signed-64 sequence exhaustion refusal;
- real Godot 4.7.2 R06 render participation;
- exact-head CI and frozen R06 source identity.

Do not treat the standalone `alpha50_transition_campaign.gd` hard-coded graph as production transition authority.

## Required work

### A — Unify live embodiment and transition qualification

Remove the parallel acceptance-only transition graph as authority.

The 10,000-case campaign and ordinary live intent path must call the **same production resolver/director/controller code path** that maps accepted body-neutral intents to frozen R06 presentation tracks.

Requirements:

- derive legal routes from the actual production transition authority/metadata;
- no duplicate hard-coded graph in a qualification-only script;
- no hard-coded Rust `action -> track 00/05` shortcut as the production decision;
- preserve controller-owned movement and one presentation clock;
- execute concrete frozen tracks in Godot 4.7.2;
- record actual route/track sequence, entry/exit state, cancellation/interruption and final state;
- unsupported/no-path requests fail closed;
- project legality must be checked before any Godot state-machine travel that could otherwise teleport.

Run at least 10,000 deterministic requests through this shared production path.

Independent verification must derive expected legality from frozen authority/metadata rather than duplicate the runtime switch statement.

Required mutations: edge removal, illegal edge insertion, facing/posture mismatch, interruption-range mutation, pack/source mutation, runtime-success/observed-route mismatch, stale/wrong result correlation, post-rejection recovery.

### B — Promote or replace shared V2 canonical types

Resolve the ambiguous V2 boundary.

For `Goal`, `Commitment`, `MemoryRecord`, `EvidenceRef`, and `BodyNeutralIntent`:

- either formally promote them to version-neutral canonical types with explicit invariant/schema/wire tests proving suitability for V2;
- or introduce V2-native types and migrate only isolated qualification/candidate state.

Do not destructively migrate potentially lived data.

### C — Resident paired causal controls

Through real processes only:

`sensor-gateway -> authenticated ordinary channel -> companion-core -> production Godot -> observed result`

Run paired controls where all non-target state is equal.

Prove different later production behavior for:

1. remembered preference;
2. corrected preference/belief while preserving superseded history;
3. pending unfinished commitment;
4. learned outcome/skill from a real Godot result.

Publish causal terms and state diffs. Direct helper calls are insufficient.

### D — Post-restart remembered behavior

After a real causal interaction:

- stop companion-core and Godot;
- restart both;
- restore same identity/epoch;
- inject later authenticated context;
- show the remembered/learned state changes the later resident action versus a paired control;
- execute the changed action in real Godot.

### E — Operative bounded organism behavior

Implement and qualify:

- drive-derived goal proposal;
- goal arbitration and continuation;
- goal completion/pause/resume;
- commitments completed/expired/revoked;
- action consequences including rest/satiation/recovery;
- temporal/supersession/scope/confidence-aware retrieval;
- unresolved contradiction -> abstention;
- evidence-gated development promotion;
- time-only promotion rejection;
- V2 consolidation with auditable episode closure, contradiction/generalization, skill/preference/routine proposals, unfinished-goal carry-forward, and curiosity/question proposals where evidence supports them;
- explicit dream/rehearsal synthetic boundary and dream->fact rejection negative.

### F — Rich isolated 30-day V2 campaign

Use actual V2 production logic in an isolated store.

Must demonstrate non-degenerate behavior, including multiple action classes and changing drive/goal regimes.

Include present/absent periods, rest cycles, commitments across lifecycle states, two or more corrections, real/simulated isolated outcome feedback, skill progression, development positive/negative, consolidation cycles, contradiction/abstention, unfinished-goal resumption, and model-worker absence.

Report distributions and transitions. Simulation is qualification evidence only.

### G — 500+ multi-class failures

Replace repeated restart dominance with at least 500 deterministic cases distributed across materially different failure classes:

- before event transaction;
- after event commit before snapshot;
- after snapshot before result acknowledgment;
- ordinary evidence processing;
- intent/result processing;
- companion SIGKILL;
- real Godot loss;
- care-core loss;
- supervisor restart where supported;
- store unavailable;
- injected corruption;
- interrupted backup/restore where safe;
- model-worker loss/absence where applicable.

Verify no reseed, duplicate event/outcome application, silent commitment loss, provenance corruption or cross-authority writes. Unrecoverable corruption must degrade/freeze explicitly.

### H — Care process-loss independence

Using the real direct-care path:

- process a candidate with companion running;
- kill companion and process a new candidate successfully;
- restore companion;
- kill care-core and prove ordinary companion life continues with explicit care degradation and no assistance-success claim;
- restart care and prove coverage recovery without companion memory/goal mutation.

No external notification.

### I — Resident clean-root restore

After nontrivial V2 history exists:

- use SQLite Backup API;
- restore to clean isolated XDG root;
- boot actual resident companion-core from restored root;
- boot/reconnect real Godot;
- verify identity, epoch, internal variables, memories/supersession, goals, commitments, learned outcomes, skills, development, and next legal sequence;
- execute a post-restore action through real Godot.

### J — Model-worker independence

If a model worker process exists, remove/kill it during qualification. If none exists, prove from actual topology that core life/memory/action/persistence/Godot intent generation has no model-worker dependency. Do not invent a fake worker solely for the test.

### K — Governed target-host sensor evidence

The handoff reports real host camera/mic acquisition. Publish sanitized governed evidence containing device identifiers, formats, timing/freshness, capture success/failure, and low-level brightness/change/audio-level metrics while preserving ephemeral raw media.

Do not claim semantic vision, person recognition, STT or TTS.

### L — Evidence integrity

Fix the artifact manifest temp-file bug: do not include the temporary output file in the manifest.

Artifacts must contain raw/reconstructable evidence for:

- ordinary authorization/replay negatives;
- shared production Godot routing;
- paired causal comparisons;
- remembered post-restart comparison;
- rich 30-day campaign;
- multi-class failures;
- care loss;
- restore;
- sensor host evidence.

## Openbox

Do not reconfigure GNOME/mutter. The dedicated 1366x768 Openbox endurance remains BLOCKED unless already available without host reconfiguration. Its absence does not stop the central software work.

## Do not change

No new art/source pixels, new runtime dependency without Architect review, cloud authority, biometric identity, live notification, model-owned canonical state/safety, PR #9 merge, Issue #8 closure, or roadmap self-acceptance.

## Stop and return to Architect only if

- potentially lived data requires destructive migration;
- Architecture v1.0 authority must change;
- an unavoidable central new runtime dependency is required;
- accepted art must change;
- required evidence needs prohibited host reconfiguration;
- a second canonical writer is unavoidable;
- safety would need to route through companion.

A failed test, missing implementation, or negative result is not a stop condition by itself.

## Acceptance criteria

C02 passes only when one exact executable tree proves:

1. live intents and 10k transition campaign share the same production Godot resolver/director path;
2. no qualification-only duplicate graph owns legality;
3. shared/V2 canonical types have an explicit accepted contract;
4. all four resident paired causal comparisons pass;
5. post-restart remembered evidence changes real resident behavior;
6. Godot loss/reconnect preserves organism truth;
7. goals/development/retrieval/consolidation are causally operative;
8. rich 30-day V2 behavior is non-degenerate;
9. 500+ genuine multi-class failure cases pass their invariants;
10. care works without companion and companion degrades explicitly without care;
11. resident clean-root restore succeeds and executes through Godot;
12. model-worker independence is topology-qualified;
13. sanitized target-host camera/mic evidence is governed or exact blockers are recorded;
14. artifact manifest is internally correct;
15. Phase 01, focused R06, inherited Phase 02 and C02 exact-head CI are green;
16. raw evidence supports independent reconstruction.

A complete pass is expected to support an Architect completion assessment around **52–55%**, assigned only after independent review.

## Required project updates

Append-only update `.agent/CURRENT.md`, `.agent/INDEX.md`, C02 implementation/results, capability matrix, Notion coder report, PR #9 and Issue #8. Preserve all prior reviews/failures.

## Required handoff

Return exactly:

# CODEX RESULT — COMPANION-ALPHA50-003-LIVE-001-C02

with sections:

## Verdict
## Authority sync
## Protected work
## Baseline / merge / branch
## Retained C01 evidence
## Shared V2 canonical type disposition
## Unified production Godot resolver/director
## 10,000-case production transition campaign
## Resident paired causal comparisons
## Post-restart remembered-action proof
## Godot-loss continuity
## Goals / development / retrieval / consolidation
## Rich V2 30-day campaign
## Seeded multi-class failure campaign
## Care process-loss independence
## Resident clean-root restore
## Model-worker independence
## Governed webcam / microphone evidence or blockers
## Openbox blocker / endurance if available without reconfiguration
## Exact-head CI
## Raw artifacts / independent verifiers / hashes
## Corrected capability matrix
## Files changed
## Failures / blockers
## Notion / GitHub publication
## Commits / remote equality
## Recommendation

Then stop for Architect review.