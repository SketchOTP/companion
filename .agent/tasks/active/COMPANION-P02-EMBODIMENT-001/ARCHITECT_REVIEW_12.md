# Architect Review 12 — R05 Sprite Generation Accepted; Technical Integration Next

## Verdict

`ACCEPTED — R05-AUTHOR-002 SPRITE-GENERATION / VISUAL-REVIEW SCOPE COMPLETE`

- Reviewed task head: `5f538a0c86783b7c5d00b140dcc91c7f76c30450`
- PR #9: draft/open/unmerged
- Issue #8: open
- Phase 02: active/not accepted
- Phase 03+: closed
- Notion review: https://app.notion.com/p/3da833cb27ff81378114f3fbdb09d6a5

The operator's final visual decision closes further unsolicited sprite generation for the delivered R05 review scope.

## Frozen review identity

- ZIP: `full-animation-review-listen-2026-09-12-v2.zip`
- ZIP SHA-256: `45fd9749179419339046af2ab40605c47d825ca4f0ad47c87a8c6fb9ca1b799b`
- Manifest SHA-256: `d8a0277272f0ccd6f948a24153b7f954aff138111ab840f22e09204aa359e595`
- HTML SHA-256: `7bd9e0cd5c64b89259dc2780825457a16144cebe02d259fd73a644591aa38cd2`

Accepted visually: selected eight-facing construction, delivered left/right movement review, front presence, connected Listen -> Acknowledge, and the operator-selected black presentation. Diagnostics and rejected studies remain rejected and runtime-ineligible.

## Count-floor reconciliation

Review 11 drawing budgets are no longer hard acceptance floors for this exact frozen visual set. Actual drawing counts remain evidence and may not be inflated by holds or repeated cycles. Future animation growth is driven by organism behavior and visible repetition needs, not arbitrary quota completion.

The older `32 families / 256 drawings` Phase-02 floor is also superseded as a proxy acceptance target. Phase 02 may close on an operator-approved seed embodiment library once the real integration, grounding, transition and endurance gates pass.

## Black visual-master ruling

The accepted masters are native 1254x1254 RGB black-backed files. They are accepted as immutable visual-source authority for the iteration-one black habitat. They are not `MON_FRAME_V1`; the existing transparent 1024x1024 RGBA contract remains intact for transparent assets.

The next gate must add a separately versioned opaque-black profile while preserving every accepted master byte. Placement uses metadata plus Godot transform/offset, not destructive source editing. No cutout, recolor, repaint, resample or new image generation is authorized.

# CODEX DIRECTIVE — COMPANION-P02-EMBODIMENT-001-R06-INTEGRATE-001

## Objective

Freeze the exact operator-approved R05 visual masters and production selection, integrate them into the real typed/Rust/Godot embodiment path under a versioned opaque-black profile, and prove truthful real-art landmarks, world contacts, events, failure behavior and export/restore. Generate no new character artwork.

## Required work

1. Merge current `origin/main` normally into `codex/p02-embodiment-001`.
2. Re-hash the local ZIP, manifest, HTML and selected sources. Stop if declared hashes differ.
3. Publish a durable hash-bound artifact and a sanitized production-selection manifest.
4. Separate approved runtime sources from diagnostics/rejected studies.
5. Add an opaque-black source/runtime profile without weakening `MON_FRAME_V1`; preserve native 1254x1254 RGB master bytes.
6. Annotate facing, posture, gait phase, root, head/eye/mouth/hand/foot landmarks, contacts, events, completion, interruptions and exact 24 Hz timing without editing pixels.
7. Add a 24 Hz actor-root translation plan for translating locomotion and require composed world contact slip <=2 px over every nontrivial planted span.
8. Run the exact accepted pack through schema, Rust typed round-trip and production Godot selection/playback/event/failure/recovery paths.
9. Render over the operator-selected black habitat and fail on a visibly distinguishable full-frame border rather than hiding it through source mutation.
10. Produce fresh local export/restore and durable hosted evidence. Defer atlas optimization unless direct-frame playback fails resource/performance limits.
11. Return one complete result and stop. Do not generate more art, start Phase 03, or run final transition/Openbox endurance in this directive.

## Acceptance criteria

- frozen hashes match;
- production selection is unambiguous and excludes diagnostics;
- visual-master source bytes are unchanged;
- opaque profile is separately versioned;
- real accepted art passes schema/Rust/Godot consumption;
- real frame landmarks/contacts are bound and world-space stance slip passes;
- event/timing/completion behavior matches accepted action order;
- corrupt/missing/ineligible behavior fails closed and restoration recovers;
- export/restore and hosted hashes pass;
- Phase 01 and Phase 02 regression remain green;
- no new character pixels are generated.

## Stop if

Stop and return to Architect if source hashes differ, selection is ambiguous, accepted art must be repainted/resized/cut out, world-contact QA cannot pass without altering art, opaque rendering shows an unacceptable border, a new dependency is needed, or accepted runtime evidence would have to be weakened.

## Capability boundary

This accepts the sprite-generation visual scope only. Phase 02 runtime integration, world grounding, transition qualification, Openbox endurance, organism capability and Phase 03+ remain unaccepted.
