# Black motion retry — incomplete, not an approval package

Seven generated studies: four inhale intermediates, two gait attempts and one
localized gait repair. Eleven exact-copy raw images including four prior keys
are in the local versioned export. Prompts and hashes are committed here.

The late gait attempt swaps the supporting leg. The localized repair improves
one overlap but does not finish or qualify the gait. Inhale contour progression
fails the explicit read-only diagnostic; pixel-audit.json retains its values.
All12 required motion tracks remain incomplete. Do not treat these diagnostics
as completed animations, runtime-ready source, or operator-approved art.

Reproduce from the exact raw corpus using audit_black_motion_retry.py (currently
exit1) and export_black_motion_review.py. Requests bind source hashes and ticks.
Normal/quarter GIFs are review derivatives only, with24Hz source timing.
No source pixels were edited by those scripts. The built-in imagegen prompts
are in prompts.json; previous approved sources and failed evidence are unchanged.
