# R05 runtime visual master selection

Immutable candidate runtime sources derived from the exact operator-approved
R05 final playback archive. Earlier construction provenance and retries are
excluded from runtime selection. Sources remain `candidate` until technical
integration and later Architect acceptance.

Native 1254x1254 RGB black-field bytes are preserved exactly. No cutout,
recolor, resample, repaint, or repair is performed; `MON_FRAME_V1` is unchanged.
