# Cutout investigation only — not an animation approval package

The selected contrast sheet compares three Photoroom source-authoring cutouts
over white, black, gray and magenta. It is a diagnostic derivative, not approved
art, not a runtime asset, and not a claim of smooth animation. All source studies
remain unqualified. Bulk generation attempts stay outside ordinary Git.

The operator rejected the earlier R05-v1 pack. The new study attempts and exact
hashes are recorded in the active packet's `r05-v2-attempts.json`. Three provider
outputs passed transport/dimensions/RGBA checks, but have alpha1/2 residue at a
few perimeter samples and are not MON_FRAME_V1 compliant without explicit
source-authoring cleanup. No such cleanup is concealed inside immutable intake.

The review-timing fix is independent: it uses actual source ticks and once/loop
completion. Six focused tests pass; three cutout-adapter test methods cover five
offline scenarios. Neither test suite can approve anatomy or motion.
